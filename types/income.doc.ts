export interface IncomeDocument {
    amount: number,
    createdAt: Date,
    description: string
    id: string
}

export interface AddIncomeDocument{
    amount: number,
    createdAt: Date,
    description: string
}